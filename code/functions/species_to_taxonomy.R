species_to_taxonomy <- function(reference){
     ### Converts a reference database from the format used by DADA2's
     ### assignTaxonomy to that required by assignSpecies.
     
     # Note;; should actually be the other way around so accession number is preserved
}